@Library('shared-library') _

pipeline {
    agent any

    stages {
        script {
            // Shared Library의 pipelineStages 변수를 호출하여 파이프라인 단계 로드
            pipelineStages()
        }
    }
}
